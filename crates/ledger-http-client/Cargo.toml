[package]
name = "ledger-http-client"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
ledger-api = { path = "../ledger-api" }
ureq = { version = "2", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
