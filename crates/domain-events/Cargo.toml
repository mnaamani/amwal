[package]
name = "domain-events"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
ledger-api = { path = "../ledger-api" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
